import { OrderByScorePipe } from './order-by-score-pipe';

describe('OrderByScorePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByScorePipe();
    expect(pipe).toBeTruthy();
  });
});
